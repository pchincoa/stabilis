---
layout: "base.njk"
---

<article class="section container">
  <div class="container container--narrow flow">
    <h1 class="article-title">
      {{ title }}
    </h1>
    <time>{{ date | date('DD MMM, YYYY') }}</time>

    <figure class="main-article___figure">
      <img src="{{ image }}" alt="{{ imageAlt }}" />
      <figcaption>
        {{ imageAlt }}
      </figcaption>
    </figure>

    {{ content | safe }}
  </div>
</article>
